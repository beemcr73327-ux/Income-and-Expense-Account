name = "finance-tracker"
pages_build_output_dir = "public"
compatibility_date = "2024-05-12"

# Bind the D1 database
[[d1_databases]]
binding = "DB" # The variable name used in your functions, e.g. env.DB
database_name = "finance_db" # The name of your database in Cloudflare
database_id = "YOUR_DATABASE_ID_HERE" # We'll ask the user to fill this in

# Define environment variables (like Google Sheet ID)
[vars]
GOOGLE_SHEET_ID = "1Z6xVqi_19Wb_s_E3Sf-DxQirptnA7XsH_6Nvm457L4Q"

# Note: GOOGLE_SERVICE_ACCOUNT_JSON should be added as a Secret in Cloudflare dashboard,
# not stored in plain text here.
